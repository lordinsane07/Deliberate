class TimeConstants {
  // Cooldown durations
  static const Duration firstReplyCooldown = Duration(hours: 12);
  static const Duration secondReplyCooldown = Duration(hours: 24);

  // Timers
  static const Duration countdownTick = Duration(seconds: 1);

  // UI delays
  static const Duration splashDelay = Duration(milliseconds: 500);
}
